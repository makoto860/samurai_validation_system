<!DOCTYPE html>
<html>
  <head>
    <title>H</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <h1>ホテルの宿泊アプリ</h1>
    <h1>バリデーションの実装</h1>
    <% if flash[:notice] %>
      <p class="notice"><%= flash[:notice] %></p>
    <% end %>
    <% if flash[:alert] %>
      <p class="alert"><%= alert %></p>
    <% end %>
    <ul>
      <li><%= link_to "サインアップ", :new_user_registration %></li>
      <li><%= link_to "ログイン", :new_user_session %></li>
      <li><%= link_to 'ログアウト', destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: "ログアウトしますか？" } %></li>
      <li><%= link_to "部屋一覧", :rooms %></li>
      <li><%= link_to "借りた部屋一覧", :rents_index %></li>
    </ul>
    <%= yield %>
  </body>
</html>
